node_modules/
.next/
.turbo/
.next-cache/
temp/
uploads/
!apps/crewmeld/lib/uploads/
!apps/crewmeld/lib/uploads/**
data/
!apps/crewmeld/data/
!apps/crewmeld/data/**
dist/
.env
.env.local
.env.*.local
# Generated OpenSandbox server api_key (host-side secret, never commit)
.opensandbox-server-key
*.log
tsconfig.tsbuildinfo
test-results/
playwright-report/
temp/
*.stackdump
.DS_Store
Thumbs.db
bun.lock

# CI drift check artifact (regenerated by scripts/check-schema-drift.ts)
.schema-snapshot.txt

# Local-only dev/debug + data-patch scripts under the app — one-off inspection
# and historical migrations; not part of the published mirror.
apps/crewmeld/scripts/

.cache/


# bun build output for socket bundle (see apps/crewmeld/package.json:build:socket)
apps/crewmeld/socket-bundled.js

# Local scratch directory for one-off scripts and SQL fragments
.tmp/

# Database dumps and backups
backup_*.sql
*.dump

